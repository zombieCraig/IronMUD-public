# Binaries

Additional executable targets.

| File | Purpose |
|------|---------|
| `ironmud-admin.rs` | Offline administration CLI for user/settings management |

Build with: `cargo build --release --bin ironmud-admin`
